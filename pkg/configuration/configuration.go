package configuration

var (
	// Version of the application
	Version = "NOT_SET"

	// Date equals the UTC Build time of the application
	Date = "NOT_SET"

	// Commit equals the git hash of the last commit used when building the application
	Commit = "NOT_SET"
)
